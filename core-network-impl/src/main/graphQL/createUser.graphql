mutation createUser ($name: String!, $email: String!){
    createUser(
        name: $name
        email: $email
      ) {
        success
        message
        user {
          name
          databaseId
          email
          isActive
        }
      }
}